<%= form_for(song) do |f| %>
  <% if song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(song.errors.count, "error") %> prohibited this song from being saved:</h2>

      <ul>
      <% song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="col-xs-1">
      <%= f.label :song_url  %>
    </div>
    <div class="col-xs-8">
      <%= f.text_field :song_url, class: 'form-control', placeholder: 'Enter URL' %>
    </div>
  </div>

  <div class="field">
    <%= f.label :platform %>
    <%= select_tag :platform, class: 'form-control' %>
    <%= options_for_select(@platform) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
